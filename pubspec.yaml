name: scanneo_app
description: "Escáner POS profesional para códigos de barras y/o QR, con historial de escaneos y compatibilidad con múltiples plataformas."
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  
  mobile_scanner: ^7.2.0
  http: ^1.6.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true